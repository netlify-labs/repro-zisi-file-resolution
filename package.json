{
  "name": "my-site",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "printenv",
    "build": "mkdir build && cp index.html build",
    "prelocalbuild": "rm -rf functions-build && mkdir functions-build",
    "localbuild": "zip-it-and-ship-it functions functions-build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@netlify/zip-it-and-ship-it": "^0.3.1"
  },
  "dependencies": {
    "lodash": "^4.17.15"
  }
}
